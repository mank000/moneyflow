server {
    listen 80;
    server_tokens off;

    location /static/ {
        alias /staticfiles/;
    }

    location / {
        proxy_set_header Host $http_host;
        proxy_pass http://moneyflow:8000/;
    }
}